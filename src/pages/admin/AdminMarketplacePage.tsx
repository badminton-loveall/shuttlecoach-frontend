import React from 'react';
import AdminMarketplace from '../../components/AdminMarketplace';

/**
 * AdminMarketplacePage
 * Admin page wrapping AdminMarketplace: browse every drill pack across every
 * center and status, and manage the Badminton Drills Pack directly.
 */
const AdminMarketplacePage: React.FC = () => {
  return <AdminMarketplace />;
};

export default AdminMarketplacePage;
