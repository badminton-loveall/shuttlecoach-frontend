import React from 'react';
import AdminSetReview from '../../components/AdminSetReview';

/**
 * AdminSetReviewPage
 * Admin page wrapping AdminSetReview for reviewing coach-submitted
 * Drill Sets before they are published to the marketplace.
 */
const AdminSetReviewPage: React.FC = () => {
  return <AdminSetReview />;
};

export default AdminSetReviewPage;
